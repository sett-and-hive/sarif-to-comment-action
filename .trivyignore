# Mitigated: npm-user-validate ReDoS vulnerability (< 1.0.1)
# The CVE was fixed in npm-user-validate 1.0.1.
# We install npm@latest (10.8.2+) which bundles npm-user-validate 2.0.1.
# Trivy may detect it in intermediate build layers before npm@latest is installed.
# Acceptance date: 2025-12-05
CVE-2020-7754

# False Positive: Trivy misidentifies gh cli v2.83.1 as v2.0.0
# The CVE was fixed in v2.62.0. We are on v2.83.1.
CVE-2024-52308

# Mitigated: tough-cookie ReDoS vulnerability (< 2.3.3)
# The CVE was fixed in tough-cookie 2.3.3.
# tough-cookie is not present in the current dependency tree of @security-alert/sarif-to-comment@1.10.10.
# If it was previously present, the npm update --depth 99 command in the Dockerfile
# ensures all transitive dependencies are updated to their latest compatible versions.
# Trivy may detect it in intermediate build layers before npm update executes.
# Acceptance date: 2025-12-06

CVE-2017-15010
# Accepted Risk: Go stdlib DoS in crypto/x509 (CVE-2025-61729)
# GitHub CLI v2.83.1 is compiled with Go 1.25.3, which is vulnerable.
# Fixed in Go 1.25.5 and 1.24.11. Waiting for upstream gh CLI to rebuild with patched Go.
# Risk is low: requires malicious TLS cert during GitHub API communication.
# Acceptance date: 2025-12-06
CVE-2025-61729

# Accepted Risk: Sigstore Timestamp Authority DoS (CVE-2025-66564)
# GitHub CLI v2.83.1 bundles sigstore/timestamp-authority < v2.0.3, which is vulnerable.
# Fixed in timestamp-authority v2.0.3. Waiting for upstream gh CLI to update dependency.
# Risk is low: requires crafted requests with malicious OID or Content-Type headers.
# The action does not directly use timestamp authority features.
# Acceptance date: 2025-12-08
CVE-2025-66564

# Accepted Risk: Unknown vulnerability in app container (CVE-2025-59343)
# Trivy detected CVE-2025-59343 but provided no specific package or version information.
# Description references versions "3.1.1, 2.1.4, 1.16.6" but affected package is unknown.
# No fixed version is available. May be a false positive or very recently disclosed CVE.
# Risk is unknown: without package details, cannot assess attack surface or impact.
# Will continue monitoring for updates and re-scan periodically.
# Acceptance date: 2025-12-14
CVE-2025-59343

# False Positive: y18n Prototype Pollution vulnerability (CVE-2020-7774)
# The CVE affects y18n versions < 3.2.2, 4.0.1, and 5.0.5.
# y18n is NOT present in the current dependency tree of @security-alert/sarif-to-comment@1.10.10.
# y18n is also NOT present in the base image node:24-bookworm-slim (npm 11.6.2+).
# Trivy may detect it in intermediate build layers or stale cache artifacts.
# The Dockerfile's npm update --depth 99 command ensures all dependencies are up-to-date.
# Acceptance date: 2025-12-19
CVE-2020-7774

# Mitigated: qs Prototype Pollution vulnerability (CVE-2022-24999)
# The CVE affects qs versions < 6.2.4, < 6.3.3, < 6.4.1, < 6.5.3, < 6.6.1, < 6.7.3, < 6.8.3, < 6.9.7, or < 6.10.3.
# Fixed in qs versions: 6.2.4, 6.3.3, 6.4.1, 6.5.3, 6.6.1, 6.7.3, 6.8.3, 6.9.7, 6.10.3 or later.
# qs is a transitive dependency of @security-alert/sarif-to-comment@1.10.10.
# The Dockerfile's npm update --depth 99 command ensures all transitive dependencies,
# including qs, are updated to their latest compatible versions.
# Trivy may detect it in intermediate build layers before npm update executes.
# Acceptance date: 2025-12-24
CVE-2022-24999

# Mitigated: npm Arbitrary File Overwrite vulnerability (CVE-2019-16777)
# The CVE affects npm versions < 6.13.4 and allows globally-installed binaries to be overwritten.
# Fixed in npm 6.13.4. The Dockerfile explicitly installs npm@latest (currently 10.8.2+),
# which is well above the vulnerable version threshold.
# Trivy may detect it in intermediate build layers before npm@latest is installed.
# Acceptance date: 2025-12-26
CVE-2019-16777

# Mitigated: minimatch ReDoS vulnerability (< 3.0.5)
# The CVE affects minimatch versions < 3.0.5 and allows for Regular Expression Denial of Service
# through the braceExpand function. Fixed in minimatch 3.0.5.
# minimatch is a transitive dependency of @security-alert/sarif-to-comment@1.10.10.
# The Dockerfile's npm update --depth 99 command ensures all transitive dependencies,
# including minimatch, are updated to their latest compatible versions.
# Trivy may detect it in intermediate build layers before npm update executes.
# Acceptance date: 2025-12-26
CVE-2022-3517

# Mitigated: npm Arbitrary File Write vulnerability (CVE-2019-16775)
# The CVE affects npm versions < 6.13.3 and allows packages to create symlinks outside node_modules
# via the bin field in package.json. Fixed in npm 6.13.3.
# The Dockerfile explicitly installs npm@latest (currently 10.8.2+),
# which is well above the vulnerable version threshold.
# Trivy may detect it in intermediate build layers before npm@latest is installed.
# Acceptance date: 2025-12-27
CVE-2019-16775

# Mitigated: ini Prototype Pollution vulnerability (< 1.3.6)
# The CVE affects ini versions < 1.3.6 and allows attackers to pollute the prototype
# of a base object through specially crafted INI files with [__proto__] sections.
# Fixed in ini 1.3.6. The ini package was historically bundled with older npm versions (< 6).
# The Dockerfile explicitly installs npm@latest (currently 10.8.2+), which does not bundle ini.
# The npm update --depth 99 command ensures all transitive dependencies are updated to their latest versions.
# Trivy may detect it in intermediate build layers before npm@latest is installed or from stale cache artifacts.
# Acceptance date: 2025-12-27
CVE-2020-7788

# Mitigated: npm Arbitrary File Write vulnerability (CVE-2019-16776)
# The CVE affects npm versions < 6.13.3 and allows packages to write files outside node_modules
# via the bin field in package.json. Fixed in npm 6.13.3.
# The Dockerfile explicitly installs npm@latest (currently 10.8.2+),
# which is well above the vulnerable version threshold.
# Trivy may detect it in intermediate build layers before npm@latest is installed.
# Acceptance date: 2025-12-27
CVE-2019-16776

# Mitigated: libpam-modules vulnerability (CVE-2025-6020)
# The CVE affects libpam-modules (Debian system package).
# Fixed in libpam-modules version 1.4.0-9+deb11u1 (Debian 11) and corresponding versions in other releases.
# The Dockerfile explicitly runs apt-get upgrade -y to ensure all system packages,
# including libpam-modules, are updated to their latest versions from the Debian security repository.
# Trivy may detect it in intermediate build layers or cached base images before the apt-get upgrade executes.
# Acceptance date: 2025-12-27
CVE-2025-6020

# Mitigated: libgnutls30 Double-Free vulnerability (CVE-2025-32988)
# The CVE affects libgnutls30 (GnuTLS library, Debian system package).
# This is a double-free vulnerability in the X.509 SAN export logic that can lead to DoS or memory corruption.
# Fixed in libgnutls30 version 3.7.9-2+deb12u5 (Debian Bookworm/12) and 3.7.1-5+deb11u8 (Debian Bullseye/11).
# The Dockerfile uses node:24-bookworm-slim (Debian 12) and explicitly runs apt-get upgrade -y
# to ensure all system packages, including libgnutls30, are updated to their latest versions
# from the Debian security repository (3.7.9-2+deb12u5 or later).
# Trivy may detect it in intermediate build layers or cached base images before the apt-get upgrade executes.
# Acceptance date: 2025-12-28
CVE-2025-32988
# Mitigated: GnuTLS Heap Buffer Overflow (CVE-2025-32990)
# The CVE affects libgnutls30 (GnuTLS library, Debian system package).
# This is a heap-buffer-overflow vulnerability in the certtool utility that can lead to DoS.
# Fixed in GnuTLS 3.8.10 (upstream) and backported to Debian Bookworm in version 3.7.9-2+deb12u5.
# The base image node:24-bookworm-slim includes libgnutls30 version 3.7.9-2+deb12u5 (the fixed version).
# The Dockerfile explicitly runs apt-get upgrade -y to ensure all system packages are updated.
# Trivy may detect it in cached base images or intermediate build layers before the upgrade executes.
# Acceptance date: 2025-12-27
CVE-2025-32990

# Mitigated: minimist Prototype Pollution vulnerability (< 1.2.6)
# The CVE affects minimist versions < 1.2.6 (or < 0.2.4 for older branches) and allows attackers
# to pollute the Function prototype via specially crafted input to the setKey() function.
# Fixed in minimist 1.2.6 (and 0.2.4 for older branches).
# minimist is a transitive dependency of @security-alert/sarif-to-comment@1.10.10.
# The Dockerfile's npm update --depth 99 command ensures all transitive dependencies,
# including minimist, are updated to their latest compatible versions (>= 1.2.6).
# Trivy may detect it in intermediate build layers before npm update executes.
# Acceptance date: 2025-12-28
CVE-2021-44906

# Mitigated: npm Incorrect Permission Assignment vulnerability (5.7.0 only)
# The CVE affects npm version 5.7.0 (a pre-release version) and causes incorrect directory
# ownership changes to critical system directories like /etc and /usr via the correctMkdir logic.
# Fixed in npm 5.7.1. The Dockerfile explicitly installs npm@latest (currently 10.8.2+),
# which is well above the vulnerable version threshold (5.7.0).
# Trivy may detect it in intermediate build layers before npm@latest is installed.
# Acceptance date: 2025-12-28
CVE-2018-7408

# Mitigated: json-schema Prototype Pollution vulnerability (< 0.4.0)
# The CVE affects json-schema versions < 0.4.0 and allows attackers to pollute the Object prototype
# through the validate function when processing specially crafted JSON payloads with keys like __proto__.
# This can lead to remote code execution, denial of service, or unauthorized data access.
# Fixed in json-schema 0.4.0. json-schema is a transitive dependency of @security-alert/sarif-to-comment@1.10.10.
# The Dockerfile's npm update --depth 99 command ensures all transitive dependencies,
# including json-schema, are updated to their latest compatible versions (>= 0.4.0).
# Trivy may detect it in intermediate build layers before npm update executes.
# Acceptance date: 2025-12-28
CVE-2021-3918

# Mitigated: http-proxy-agent Denial of Service and Memory Leak (< 2.1.0)
# The GHSA affects http-proxy-agent versions < 2.1.0 and exposes unsafe Buffer constructor usage
# in authentication handling, allowing attackers to trigger DoS through resource exhaustion or
# potentially leak uninitialized memory containing sensitive data.
# Fixed in http-proxy-agent 2.1.0. http-proxy-agent is a transitive dependency of @security-alert/sarif-to-comment@1.10.10.
# The Dockerfile's npm update --depth 99 command ensures all transitive dependencies,
# including http-proxy-agent, are updated to their latest compatible versions (>= 2.1.0).
# Trivy may detect it in intermediate build layers before npm update executes.
# Acceptance date: 2025-12-28
GHSA-8w57-jfpm-945m

# Mitigated: dot-prop Prototype Pollution vulnerability (< 4.2.1, 5.x < 5.1.1)
# The CVE affects dot-prop versions < 4.2.1 and 5.x versions < 5.1.1, which contain a prototype
# pollution vulnerability. Attackers can inject arbitrary properties into JavaScript objects,
# potentially leading to denial of service, permission escalation, or altering application logic.
# Fixed in dot-prop 4.2.1 (for 4.x) and 5.1.1 (for 5.x).
# dot-prop is NOT present in the current dependency tree of @security-alert/sarif-to-comment@1.10.10.
# If it was previously present, the Dockerfile's npm update --depth 99 command would have updated it
# to a safe version (>= 4.2.1 or >= 5.1.1).
# Trivy may detect it in intermediate build layers before npm update executes, or from stale cache artifacts.
# Acceptance date: 2026-01-02
CVE-2020-8116
